// 此文件由 pnpm run sync:version 生成；请修改 package.json version 后重新同步。
// launcher 和认证页都从这里读取版本，避免运行时解析 package.json。

export const OPENCLAUDE_VERSION = "0.2.0";
export const OPENCLAUDE_VERSION_LABEL = `v${OPENCLAUDE_VERSION}`;
